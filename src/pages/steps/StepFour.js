import React from "react";
import { withRouter } from "react-router-dom";

const StepFour = (props) => {

  return (
    <div className="succes__screen">
      <p className="succes__title">Tu pago fue exitoso!</p>
    </div>
  );
};

export default withRouter(StepFour);
